{
  "name": "layin",
  "description": "An application inspired to Windows Power Toys to create a layouts on screens to place windows on, simply pressing a key and dragging it.",
  "homepage": "https://github.com/jasoc/layin",
  "author": "Paride Giunta <paridegiunta@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git://github.com/jasoc/layin.git"
  },
  "version": "0.0.1",
  "scripts": {
    "start": "run-p gui:start app:start",
    "watch": "run-p gui:start app:watch",
    "build": "npm run gui:build && npm run app:build",
    "gui:start": "cd gui && npm run start",
    "app:start": "cd app && npm run start",
    "app:watch": "cd app && npm run watch",
    "gui:build": "cd gui && npm run build",
    "app:build": "cd app && npm run build"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
